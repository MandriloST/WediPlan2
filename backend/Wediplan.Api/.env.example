# Backend env primjer (Faza 3). Kopiraj vrijednosti u okruženje ili appsettings.*.json.
# NIKAD ne commitati stvarne ključeve.
WEDIPLAN_DB=Host=localhost;Port=5433;Database=wediplan;Username=wediplan;Password=wediplan
App__PublicUrl=http://localhost:3000
# Email__ResendApiKey=re_xxx           # prazno → dev konzola
# Email__From=Wediplan <no-reply@wediplan.hr>
# Google__ClientId=xxx.apps.googleusercontent.com
# Google__ClientSecret=xxx
# Auth__CookieDomain=.wediplan.hr      # samo produkcija
